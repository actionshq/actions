# yaml-language-server: $schema=../Action.schema.json
apiVersion: make.new/v1alpha1
kind: Action
metadata:
  name: Event
spec:
  slug: event
  icon:
    type: FontAwesomeIcon
    name: calendar
  services: []
